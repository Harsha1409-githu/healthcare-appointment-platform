export default function AvailabilityTipCard() {
  return (
    <section className="mt-3 rounded-2xl border border-cyan-100 bg-cyan-50 px-3 py-3">
      <p className="text-xs font-black text-cyan-800">
        💡 Create sessions like Morning Clinic, Evening Video, or Full Day Both.
        Each generated slot can accept only one patient.
      </p>
    </section>
  );
}